package test.dor.iso {
    import com.dor.iso.*;
    import flash.display.Sprite;

    public class IsoViewTest extends Sprite {
        private var v : IsoView = new IsoView();

        public function IsoViewTest() : void {
            createLayer();    
        }

        private function createLayer() : void {
        }
    }
}
